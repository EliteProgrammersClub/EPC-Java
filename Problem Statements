java program to reverse an array

and the user should determine the number of values in the array

program to determine minimum and max values of the array and it determines duplicate values in the array

do that for strings too.

common elements between 2 arrays

a program to remove duplicate elements in the array.

sum of two matrices

a program to compare two strings lexicographically.

a user enters a string (jark) and a number the program replaces the element at that position number with a *.
